{
  "name": "slack-matrix-bridge",
  "version": "0.1.0",
  "description": "Slack to Matrix messaging bridge on Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run wrangler dev",
    "deploy": "bun run wrangler deploy",
    "tail": "bun run wrangler tail",
    "typecheck": "bun run tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [
    "slack",
    "matrix",
    "bridge",
    "cloudflare",
    "workers"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "hono": "^4.6.12"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.5",
    "@cloudflare/workers-types": "^4.20241227.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.7",
    "wrangler": "^4.9.0"
  }
}
